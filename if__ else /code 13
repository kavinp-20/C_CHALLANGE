#include <stdio.h>

int main() {
    char password[50];

    printf("Enter password: ");
    scanf("%s", password);

    if ((password) >= 8)
        printf("Strong Password");
    else
        printf("Weak Password");

    return 0;
}
